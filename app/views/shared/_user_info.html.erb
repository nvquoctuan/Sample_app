<%= link_to gravatar_for(current_user, size: Settings.size.s_50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".title_link"), current_user %></span>
<span><%= pluralize "", t(".count", count: current_user.microposts.count) %></span>
